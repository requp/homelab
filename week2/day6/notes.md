# Results for day 6
### 1. Fixed a problem with ubuntu target (used wrong sudo package (sudo-rc) by default)
### 2. Practiced with handler and template statements
### 3. Made a playbook to install docker on 4-5 linux distributions (ubuntu, fedora, debian, almalinux, centos (didn't check last one but almalinux uses centos' docker repository))
### 4. Made a playbook to change a config file with a template on 4 distirbutions
