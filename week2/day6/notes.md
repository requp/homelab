# Results for day 6
### 1. Fixed a problem with the Ubuntu target (it used a incorrect sudo package (sudo-rc) by default)
### 2. Practiced with handler and template statements
### 3. Made a playbook to install Docker on 4-5 Linux distributions (Ubuntu, Fedora, Debian, AlmaLinux, CentOS (didn't check the last one but AlmaLinux uses the CentOS's docker repository))
### 4. Made a playbook to change a config file using a template on 4 distributions
