---

- hosts: all
  become: true
  tasks:
    - name: "apt update"
      apt: 
        update_cache: yes
        state: latest

    - name: "install nginx package"
      apt:
        name: nginx  
        state: latest    
