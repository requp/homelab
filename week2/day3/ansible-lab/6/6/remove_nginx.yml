---

- hosts: all
  become: true
  tasks:
    - name: "apt update"
      apt: 
        update_cache: yes
        state: absent

    - name: "remove nginx package"
      apt:
        name: nginx  
        state: absent    
